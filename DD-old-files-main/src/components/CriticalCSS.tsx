// Critical CSS is now inlined in index.html for maximum performance
// This component is no longer needed but kept for backwards compatibility
const CriticalCSS = () => null;

export default CriticalCSS;